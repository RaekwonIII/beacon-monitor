{
  "name": "beacon-monitor",
  "module": "index.ts",
  "version": "1.0.0",
  "description": "Validator monitor for Ethereum beacon nodes",
  "type": "module",
  "bin": {
    "beacon-monitor": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc",
    "monitor": "bun --watch src/main.ts",
    "generate": "bun --watch src/generate.ts",
    "deposit": "bun --watch src/deposit.ts",
    "start": "node dist/main.js"
  },
  "keywords": [
    "beacon",
    "ethereum",
    "validator",
    "monitor"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "@types/node": "^20.10.0",
    "bun-types": "^1.1.22"
  },
  "dependencies": {
    "@ssv-labs/ssv-sdk": "^0.1.3",
    "viem": "^2.45.1"
  }
}
